import { CalendarDays, HeartPulse, LayoutDashboard, ServerCog, X } from 'lucide-react';
import { NavLink } from 'react-router-dom';
import { cn } from '@/lib/cn';
const links = [{ to: '/dashboard', label: 'Çağrı paneli', icon: LayoutDashboard }, { to: '/appointments', label: 'Randevular', icon: CalendarDays }, { to: '/system', label: 'Sistem ve loglar', icon: ServerCog }];
export function Sidebar({ open, onClose }: { open: boolean; onClose: () => void }) { return <><button type="button" aria-label="Menüyü kapat" onClick={onClose} className={cn('fixed inset-0 z-30 bg-navy-900/50 backdrop-blur-sm lg:hidden', open ? 'block' : 'hidden')} /><aside className={cn('fixed inset-y-0 left-0 z-40 flex w-72 flex-col bg-navy-900 px-4 py-5 text-white transition-transform lg:translate-x-0', open ? 'translate-x-0' : '-translate-x-full')}><div className="flex items-center justify-between px-2"><div className="flex items-center gap-3"><span className="rounded-xl bg-aqua-500/20 p-2.5 text-aqua-100 ring-1 ring-aqua-100/20"><HeartPulse size={22} /></span><div><p className="font-semibold">Operasyon Merkezi</p><p className="text-[11px] text-slate-400">Hastane Agentic AI</p></div></div><button className="rounded-lg p-2 text-slate-400 hover:bg-white/10 lg:hidden" onClick={onClose} aria-label="Menüyü kapat"><X size={20} /></button></div><nav aria-label="Ana menü" className="mt-10 space-y-1">{links.map(({ to, label, icon: Icon }) => <NavLink key={to} to={to} onClick={onClose} className={({ isActive }) => cn('flex items-center gap-3 rounded-xl px-3 py-3 text-sm font-medium transition', isActive ? 'bg-white text-navy-900 shadow-sm' : 'text-slate-300 hover:bg-white/10 hover:text-white')}><Icon size={18} />{label}</NavLink>)}</nav><div className="mt-auto rounded-2xl bg-white/5 p-4 ring-1 ring-white/10"><div className="flex items-center gap-2 text-xs font-semibold text-aqua-100"><span className="h-2 w-2 rounded-full bg-emerald-400" /> Güvenli görünüm</div><p className="mt-2 text-xs leading-5 text-slate-400">Kişisel veriler panel genelinde maskeli gösterilir.</p></div></aside></>; }
