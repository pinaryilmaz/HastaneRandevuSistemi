import { LogOut, Menu, ShieldCheck, UserRound } from 'lucide-react';
import { useNavigate } from 'react-router-dom';
import { Button } from '@/components/ui/button';
import { useAuthStore } from '@/features/auth/store/authStore';
import { ConnectionBadge } from '@/features/system/components/ConnectionBadge';
export function Header({ onMenu }: { onMenu: () => void }) { const user = useAuthStore((state) => state.user); const clear = useAuthStore((state) => state.clearSession); const navigate = useNavigate(); return <header className="sticky top-0 z-20 flex h-20 items-center justify-between border-b border-slate-200/80 bg-slate-50/90 px-4 backdrop-blur sm:px-7 lg:px-8"><div className="flex items-center gap-3"><Button variant="ghost" size="sm" className="px-2 lg:hidden" onClick={onMenu} aria-label="Menüyü aç"><Menu size={21} /></Button><ConnectionBadge /></div><div className="flex items-center gap-3"><span className="hidden items-center gap-1.5 rounded-full bg-emerald-50 px-3 py-1.5 text-xs font-medium text-emerald-700 sm:inline-flex"><ShieldCheck size={13} /> Yetkili oturum</span><div className="hidden text-right sm:block"><p className="text-sm font-semibold text-slate-800">{user?.name}</p><p className="text-xs text-slate-400">Operasyon kullanıcısı</p></div><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-navy-900 text-white"><UserRound size={18} /></span><Button variant="ghost" size="sm" className="px-2" aria-label="Çıkış yap" onClick={() => { clear(); navigate('/login', { replace: true }); }}><LogOut size={18} /></Button></div></header>; }
