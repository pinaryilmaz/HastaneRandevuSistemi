import type { LogLevel } from '@/api/contracts';
import { Input } from '@/components/ui/input';
import { Select } from '@/components/ui/select';
export interface LogFilterValues { service: string; level: LogLevel | ''; correlationId: string }
export function LogFilters({ value, onChange }: { value: LogFilterValues; onChange: (next: LogFilterValues) => void }) { const set = <K extends keyof LogFilterValues>(key: K, next: LogFilterValues[K]) => onChange({ ...value, [key]: next }); return <div className="grid gap-3 sm:grid-cols-3"><Select aria-label="Log servisi" value={value.service} onChange={(event) => set('service', event.target.value)}><option value="">Tüm servisler</option><option value="appointment-service">Appointment</option><option value="call-service">Call</option><option value="notification-service">Notification</option><option value="whatsapp-service">WhatsApp</option></Select><Select aria-label="Log seviyesi" value={value.level} onChange={(event) => set('level', event.target.value as LogLevel | '')}><option value="">Tüm seviyeler</option><option value="INFO">INFO</option><option value="WARN">WARN</option><option value="ERROR">ERROR</option></Select><Input aria-label="Correlation ID" value={value.correlationId} onChange={(event) => set('correlationId', event.target.value)} placeholder="Correlation ID ile filtrele" /></div>; }
